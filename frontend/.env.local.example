# Frontend development environment variables.
# Copy to .env.local — never commit this file.
#
# VITE_TENANT_ID is no longer required.
# The tenant is now determined by the JWT issued at login.
#
# No variables are required for development — the Vite proxy
# handles /api/* → http://localhost:3001 automatically.
